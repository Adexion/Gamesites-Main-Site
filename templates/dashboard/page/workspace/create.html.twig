{% extends 'dashboard/base.html.twig' %}

{% block full_window %}
    <main class="col-md-12 ms-sm-auto col-lg-12 px-md-4 pt-3">
        <div class="h-100 d-flex justify-content-center align-items-center">
            <div class="w-50">
                <div class="d-flex justify-content-between pb-3"><h1>Workspace creation</h1></div>
                {{ form_start(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </main>
{% endblock %}