{% extends 'security/base.html.twig' %}

{% block body %}
    <div class="d-flex justify-content-center align-items-center">
        <div class="d-flex flex-column justify-content-center align-items-center w-50 container">
            <div>
                <p>Dzień dobry {{ user.email }}</p><br/>
                <p>Na prośbę zarządcy grupy {{ workspace.name }} utworzyliśmy dla ciebie konto!</p>
                <p>Poniżej znajdziesz hasło do logowania do strony:</p>
                {% set uri = 'https://www.gamesites.pl/dashboard/' %}
                <a href="{{ uri }}">{{ uri }}</a><br/>
                <br/>
                <b>Twoje hasło to: {{ password }}</b>
                <br/>
                <p>Dziękujemy!</p>
                <p>Zespół Gamesites.pl!</p>
            </div>
        </div>

        <div class="vh-100 w-50 gradient d-flex flex-column justify-content-center align-items-center">
            <div class="gradient-image"></div>
            <h1>GameSites</h1>
            <h2>The true power of gaming...</h2>
        </div>
    </div>
{% endblock %}
