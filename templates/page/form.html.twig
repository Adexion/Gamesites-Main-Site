{% extends 'base.html.twig' %}

{% block body %}
    {% include 'component/navabr.html.twig' %}
    <section class="container">
        <div class="row justify-content-center align-items-center">
            <div class="w-50">
                {{ form(form) }}
            </div>
        </div>
    </section>
{% endblock %}